import React from 'react';
import FormBase from './container/form-base/FormBase';

const App: React.FC = () => {
    return (
        <div className="container p-3">
            <FormBase />
        </div>
    );
};

export default App;
